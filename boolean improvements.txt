default:
- split saves & options into 2 files
  - have an option ingame to get a transferrable savefile?
  - autosave options
- rebindable cutscene skip
- rebindable pause
- maybe remove arrow keys as a forced keybind
- colorblind options for color zones
- if i understand the code currently, color zones currently give 1 frame of leeway for stepping outside of a colorzone right before taking damage. not sure if this should be 4 frames now for equivalent leeway.
- adjust dash / air dash tutorial text to not focus on up/down dash as if anyone uses that lmao
- make Nightmare work properly when starting new files from a Nightmare save
- held buttons not registered on room entries
- render boss hp bars (maybe just Megaman's) to not show empty despite the boss having hp

dipswitches:
- autosave on room entry
- SOCD handling (last-pressed)
- make Final Nightmare & Shadow Form backgrounds lighter (oEvExGates line 1457)
- fix Koopa sprite bug
- non-restarting music
- let bomb start off double-tapped? 
- depth issue in death's courtyard https://discord.com/channels/193230048138887168/339363721120055298/1531699002913984712
- catstrope in gate 6 shadowy council
- make dash like how it works in DT4...? might be overstepping


actually implemented:
1) DT4 reset
	`if (oKeyCodesHighFPS.kCode[10]=1 and oKeyCodesHighFPS.kCodePressed[14]=1 and` in oPlayer1 Step Action
	
2) ATK buffer (will probably remove)
	oPlayer1 create event (atkBuffer)
	playerTechUse script (atkBuffer)
	characterStepEvent (atkBuffer)
	
3) sigma avoidance re-added, oSigmaA step event line 43

4) megaman recharge dipswitch (I forgot the lines sorry)

5) MM Gravity Well platform bug fixed, with dipswitch
	switch is global.fixMMGravPlatforms=true in oGame create event
	applied in oGWPlatform step event, lines 94-104 and 108-136

6) fixDarkBackgrounds, oEvExGates create event line 26, oEvExGates step line 1539

7) HQ Music

8) Auto save feature. Right now it auto saves on a room transition with a 5 minute cooldown. We can add an option to tweak that cooldown or turn it off. Most of the logic for this lives in oGame's Room Start event. oGame Create has a new line initializing the auto save timer, and step increments it accordingly.